org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
kotlin.jvm.target.validation.mode=ignore
# Disable ART baseline profile compilation — the intermediate baseline-prof.txt
# is not produced when coreLibraryDesugaring is active, causing
# :app:compileReleaseArtProfile to crash with "No such file or directory".
android.experimental.art.profiles.enabled=false
